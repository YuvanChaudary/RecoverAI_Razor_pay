# ==============================================================================
# RecoverAI: Environment Configuration Template (.env.example)
# Razorpay AI Buildathon — Track 3 (AI Revenue Recovery)
# ==============================================================================
# Instructions: Copy this file to '.env' and populate values for local testing.
# NEVER commit the actual '.env' file containing real secrets to version control.
# ==============================================================================

# ------------------------------------------------------------------------------
# Application & Core Server Configuration
# ------------------------------------------------------------------------------
APP_ENV=development
APP_NAME=RecoverAI
API_HOST=0.0.0.0
API_PORT=8000
SECRET_KEY=
JWT_SECRET=

# ------------------------------------------------------------------------------
# Razorpay API Credentials (Test Environment Only)
# ------------------------------------------------------------------------------
RAZORPAY_KEY_ID=
RAZORPAY_KEY_SECRET=
RAZORPAY_WEBHOOK_SECRET=

# ------------------------------------------------------------------------------
# Database Configuration (PostgreSQL Operational Datastore)
# ------------------------------------------------------------------------------
POSTGRES_USER=recovery_admin
POSTGRES_PASSWORD=
POSTGRES_DB=recovery_db
POSTGRES_HOST=postgres
POSTGRES_PORT=5432
DATABASE_URL=postgresql+asyncpg://recovery_admin:${POSTGRES_PASSWORD}@postgres:5432/recovery_db

# ------------------------------------------------------------------------------
# Temporal Orchestration Engine
# ------------------------------------------------------------------------------
TEMPORAL_HOST=temporal:7233
TEMPORAL_NAMESPACE=default
TEMPORAL_TASK_QUEUE=recovery-task-queue

# ------------------------------------------------------------------------------
# AI Decision Engine (NVIDIA API & Model Choice)
# ------------------------------------------------------------------------------
NVIDIA_API_KEY=
NVIDIA_MODEL=meta/llama-3.1-70b-instruct
NVIDIA_BASE_URL=https://integrate.api.nvidia.com/v1

# ------------------------------------------------------------------------------
# AI Observability & Tracing (Langfuse)
# ------------------------------------------------------------------------------
LANGFUSE_PUBLIC_KEY=
LANGFUSE_SECRET_KEY=
LANGFUSE_HOST=https://cloud.langfuse.com

# ------------------------------------------------------------------------------
# Governance & RegTech Engine (Open Policy Agent)
# ------------------------------------------------------------------------------
OPA_URL=http://opa:8181/v1/data/recovery/governance/allow

# ------------------------------------------------------------------------------
# Cryptographic Audit Ledger (immudb)
# ------------------------------------------------------------------------------
IMMUDB_HOST=immudb
IMMUDB_PORT=3322
IMMUDB_USER=immudb
IMMUDB_PASSWORD=

# ------------------------------------------------------------------------------
# Notification Provider (Novu HTTP API)
# ------------------------------------------------------------------------------
NOVU_API_KEY=
